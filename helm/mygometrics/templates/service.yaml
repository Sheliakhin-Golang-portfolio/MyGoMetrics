apiVersion: v1
kind: Service
metadata:
  name: {{ include "mygometrics.fullname" . }}
  labels:
    {{- include "mygometrics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mygometrics.selectorLabels" . | nindent 4 }}
